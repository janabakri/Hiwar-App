# الأعمال المنجزة في Hiwar App

هذا الملف يلخص الأعمال الموجودة في النسخة الحالية من المستودع، ويُستخدم كمرجع مختصر قبل النشر أو تسليم المشروع.

## حالة المشروع

تم تجهيز الهيكل الأساسي لتطبيق Hiwar كتطبيق Flutter بواجهة RTL عربية، مع Backend مبني على FastAPI وقاعدة بيانات SQLAlchemy. تم الحفاظ على فصل مفاتيح API وملفات البيئة عن الكود الذي يُرفع إلى GitHub.

## الفرونت اند والشاشات

| الشاشة أو الجزء | الحالة الحالية |
|---|---|
| شاشة الترحيب | موجودة وتظهر قبل تسجيل الدخول للمستخدم الجديد، مع حفظ حالة مشاهدة الترحيب محليًا. |
| التسجيل وتسجيل الدخول | مرتبطة بطبقة API، وتدعم التسجيل بالبريد بعد التحقق، وتسجيل الدخول عبر Google/Apple عندما تكون إعداداتهما صحيحة. |
| الإعداد الأولي | شاشة لإكمال معلومات المستخدم وأهداف التعلم والمهارات والوقت اليومي. |
| الرئيسية | واجهة ذكية RTL لا تعرض أخطاء أو تقدمًا وهميًا للمستخدم الجديد، وتعرض نتيجة المستوى والنشاط بعد توفر بيانات حقيقية. |
| المحادثة الصوتية | تدفق Voice Chat مع التعرف على كلام المستخدم، إرسال النص إلى Backend، عرض رد المدرب، وتشغيل الرد عبر TTS مع اختيار صوت المدرب. |
| الاستكشاف | قسم مستقل للوصول إلى مسارات وموضوعات تدريبية إضافية. |
| التقدم | عرض نتيجة المستوى، التقدم، الجلسات، الأيام المتتالية، والإحصاءات المتوفرة للحساب. |
| حسابي | عرض الاسم والبريد والمعلومات الشخصية داخل جدول، مع صفحة منفصلة لتعديل البيانات، وإعداد صوت المدرب، وتسجيل الخروج، وحذف الحساب نهائيًا. |

## المحادثة والذكاء الاصطناعي

تم توصيل شاشة المحادثة بواجهة `/api/v1/chat` من خلال `frontend/lib/services/hiwar_api.dart`. يتم إرسال `user_id` والرسالة ومعرّف المحادثة عند توفره، ثم قراءة الرد والتصحيحات والنصائح وبيانات التحليل.

واجهة المحادثة تتعامل مع حالات الاتصال والفشل بدل عرض نتيجة وهمية. كما تم دعم افتتاحية المحادثة، تغيير موضوعات التدريب، زر تشغيل الرد يدويًا في المتصفح عند منع التشغيل التلقائي، واختيار صوت بنت أو ولد من صفحة الحساب. جودة الرد النهائي تعتمد على توفر مزود الذكاء الاصطناعي ومفتاحه في Backend، ولا يجب وضع المفتاح داخل Flutter أو GitHub.

## اختبار تحديد المستوى والنتيجة

تم تحديث تدفق اختبار تحديد المستوى داخل `frontend/lib/screens/home_screen.dart` ليشمل أقسام Grammar وVocabulary وReading وListening وSpeaking بحسب النسخة الحالية من الشاشة. تم إصلاح الانتقال من إجابة Reading وعدم بقاء الشاشة عالقة، كما تم ربط الحفظ بواجهة `/api/v1/assessment/level`.

تظهر النتيجة بعد إنهاء الاختبار في شاشة مستقلة، ويُحتسب مقدار الحلقة البنفسجية من النسبة الفعلية للمستخدم؛ فعند 50% تظهر نصف الحلقة، وعند 100% تكتمل. كما تُعرض الدرجة والمستوى في الرئيسية والتقدم والحساب بعد تحديث الملف من Backend، ولا تُعرض نتيجة غير موجودة للمستخدم الجديد.

توجد كذلك واجهات Backend لـ Reading وSpeaking، مع حفظ نتيجة المستوى في ملف المستخدم. تحليل Speaking المتقدم يحتاج مزود AI صالحًا ومفتاحًا فعالًا، بينما تقييم النطق الحقيقي عبر Azure يبقى ميزة اختيارية إلى حين إضافة إعداداته.

## المصادقة والجلسة

تمت إضافة JWT إلى طلبات Flutter المحمية، مع تخزين التوكن في `flutter_secure_storage` ومعرّف المستخدم في التخزين المحلي. تسجيل الخروج يعرض حوار تأكيد، ثم يمسح الجلسة المحلية. لا يتم اعتبار فشل Backend نجاحًا في المصادقة أو الحذف.

## حذف الحساب نهائيًا

تمت إضافة endpoint محمي:

```text
DELETE /api/v1/account
```

لا يعتمد endpoint على `user_id` مرسل من الواجهة؛ بل يحدد الحساب من JWT الحالي، مما يمنع المستخدم من طلب حذف حساب شخص آخر. عند النجاح تُحذف بيانات الحساب من قاعدة البيانات، بما في ذلك أخطاء المستخدم ومحادثاته ورسائله. عند حدوث خطأ يتم تنفيذ `rollback` ولا تُمسح الجلسة المحلية.

في Flutter، لا يستدعي زر الحذف `signOut()` مباشرة. أولًا يرسل طلب الحذف إلى Backend، وبعد تلقي استجابة ناجحة فقط يتم مسح التوكن ومعرّف المستخدم وإرجاع التطبيق إلى مسار الدخول. يتطلب الحذف كتابة كلمة «حذف» بعد ظهور تحذير واضح بأن العملية غير قابلة للتراجع.

## الاختبارات والتحقق

تمت إضافة اختبار رجعي للحذف الدائم ضمن `Backend/tests/test_api_security.py`، ويتحقق من حذف الحساب وأخطائه ومحادثاته ورسائله مع بقاء حساب مستخدم آخر. آخر تشغيل لمجموعة الاختبارات الأمنية أعطى:

```text
5 passed
```

كما نجح اختبار SQLite معزول للتأكد من أن حذف الحساب لا يحذف بيانات مستخدم آخر. تشغيل `flutter analyze` و`flutter build` يجب أن يتم على جهاز يحتوي على Flutter SDK؛ بيئة التنفيذ الحالية لا تحتوي على Flutter، لذلك لم يتم ادعاء نجاح فحص Flutter من داخلها.

## ما يلزم قبل النشر العام

| البند | المطلوب |
|---|---|
| مفاتيح AI | إضافة مفتاح Gemini أو OpenAI في Secrets الخاصة بالBackend فقط، والتحقق من الرصيد والصلاحيات. |
| البريد | إبقاء إعدادات SMTP في ملف البيئة المحلي أو Secrets وعدم رفعها إلى GitHub. |
| Google وApple | استخدام Client IDs وRedirect URIs وBundle IDs صحيحة لكل منصة قبل النشر. |
| Flutter | تشغيل `flutter analyze` و`flutter test` و`flutter build` على جهاز التطوير، ثم اختبار المايكروفون وTTS على جهاز حقيقي. |
| قاعدة البيانات | أخذ نسخة احتياطية قبل تجربة الحذف على بيانات حقيقية، وتجربة حذف حساب تجريبي فقط. |
| الأسرار والملفات المؤقتة | عدم رفع `.env` أو مفاتيح API أو `build/` أو `.dart_tool/` أو `__pycache__/`. |

## الملفات الأساسية المرتبطة بالإنجاز

- `frontend/lib/main.dart`
- `frontend/lib/screens/welcome_screen.dart`
- `frontend/lib/screens/auth_screen.dart`
- `frontend/lib/screens/onboarding_screen.dart`
- `frontend/lib/screens/home_screen.dart`
- `frontend/lib/services/hiwar_api.dart`
- `Backend/app/api/v1/profile.py`
- `Backend/app/api/v1/chat.py`
- `Backend/app/api/v1/assessment.py`
- `Backend/tests/test_api_security.py`
